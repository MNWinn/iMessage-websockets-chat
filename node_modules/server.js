const { WebSocketServer } = require('ws');

const ws_server = new WebSocketServer({port:8080});
ws_server.on('connection', client => {
  client.on('message', (message, isBinary) => {
    [...ws_server.clients].filter(c => c !== client).forEach(c => c.send(isBinary? message.toString() :message));

  });

})

